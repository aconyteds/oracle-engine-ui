# Query to get the Current User's details, requires the AuthHeader to be set
query CurrentUser {
    currentUser {
        id
        name
        lastSelectedCampaign {
            id
        }
        isActive
    }
}

query GetUsageLimits {
    currentUser {
        usageLimits {
            dailyUsage {
                limit
                current
                percentUsed
            }
            campaignUsage {
                limit
                current
                canCreate
            }
        }
    }
}
